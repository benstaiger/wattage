
from aero import conversion
from aero import wattage
from aero import wheel


_chainrings = list(range(30,63,1))
_cassettes = list(range(10,51,1))


def find_gear_ratio(speed, ftp, incline):
    pass


def find_cassette(chainring, gear_ratio):
    pass


def find_setups(ratio):
    """
    return a list of chainring / cassettes that will contain the 
    desired ratio.
    """
    pass